<div>
  <h2 style="font-weight:200;">Welcome to Hackevents</h3>
  <p class="lead">Hackevents helps you find and plan your next hackathon.</p><br/>
</div>

<div>
  <%= form_for(@user, url: interest_profile_path, method: :patch) do |f| %>

    <div class="form-inputs profile-settings-form">
      <h4 class="lead">Location</h4>
      <div class="special"><%= f.label :location, "City, Country" %><br/>
      <%= f.text_field :location,  placeholder: "City, Country" %></div>
      <div class="special"><%= f.label :radius, "Radius (in km)" %><br/>
      <%= f.number_field :radius%></div>
    </div>
    <br />

    <div class="form-actions profile-settings-form">
      <%= f.submit  "Continue", :class => "btn btn-primary" %>
    </div>
  <% end %>
</div>